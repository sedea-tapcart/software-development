---
description: >-
  Routing only: when to edit hosting-repo .cursor/rules vs software-development center rules.
  On-demand rule — load when hosting-repo .cursor/rules routing vs center rules is needed.
alwaysApply: false
---

# Maintain repo cursor rules (`.cursor/rules/`)

## How this rule is loaded

**On-demand rule.** Mission Control warm-up loads [`.sedea/centers/software-development/rules/bootstrap.mdc`](bootstrap.mdc) as the sole Software Development `alwaysApply: true` bootstrap. Read **this file** when you need hosting-repo **`.cursor/rules/*.mdc`** routing vs center rules — not on every dispatch warm-up.

This file lives under **`.sedea/centers/software-development/rules/`** and is **`alwaysApply: false`** (SD §5.4 on-demand flip). It does **not** author Sedea center governance under **`.sedea/centers/<centerSlug>/`**.

**Apply the procedures in this document only** when the task changes **hosting-repo** navigation or codebase conventions in **`.cursor/rules/*.mdc`** on the repo that ships code (the active **hosting repo** or a **hosting repo worktree**). For R&D delivery process, edit **`.sedea/centers/software-development/rules/*.mdc`** and mission assets via **`improve center rules`** / **`improve mission`** — not via this file’s edit loop alone.

## Scope

This rule applies to **domain-specific Cursor rules** under **`.cursor/rules/*.mdc`** in the **hosting repo where code changes land** — for example a Sedea-enabled **hosting repo** (root contains `.sedea/centers/sedea/`) or a **hosting repo worktree** (`user-auth`, `payments-web`, and similar). Those files help agents **navigate and understand that codebase**: architecture, conventions, call patterns, module boundaries, and repo-specific tooling — not Sedea platform governance.

### Where to edit which governance (routing)

| You need to change… | Edit here | Mission / phrase |
|---------------------|---------|------------------|
| Hosting repo **codebase navigation** (modules, APIs, conventions) | **`.cursor/rules/*.mdc`** in the repo that ships the code | Per-PR plan **`## 5. Repo rules impact`**; this rule |
| **Software Development center** delivery process (`plan and deliver`, PR shipping, planning targets) | **`.sedea/centers/software-development/rules/*.mdc`** and mission skills/plans | **`improve center rules`** on **sedea** — **`center-maintenance`** mission; include **`software-development`** (or target **`centerSlug`**) in the opening message |
| **Sedea platform** rules (hosting layout, missions, Safeguard, git gate) | **`.sedea/centers/sedea/rules/*.mdc`** | **`improve center rules`** on **sedea** — **`center-maintenance`** mission |
| A **mission plan** or **skill** under software-development | `missions/<missionSlug>/` under this center | **`improve or expand mission`** or **`fix mission`** on **sedea** — **`mission-maintenance`** mission; include **`software-development`** and the mission name in the opening message (longer phrase wins per **`.sedea/centers/sedea/rules/4_mission.mdc`**) |
| Open a **center-repo** PR for center **`rules/`**, **`docs/`**, missions, or skills | **Center git repository** **`CENTER_WORKTREE_ROOT`**; **`gh pr create --base "<defaultBranch>"`** from **`centers.yaml`** for target **`centerSlug`** | **`improve center rules`** / **`improve mission`** on **sedea**; **`development-process.md`** § *Center-repo PR base (binding)* |
| After a **center-repo** PR merges (center **`rules/`**, **`docs/`**, missions, skills) | **Hosting repo** submodule gitlink on **`main`** | Run [`.sedea/centers/sedea/skills/promote-submodule-pin/SKILL.md`](.sedea/centers/sedea/skills/promote-submodule-pin/SKILL.md) **inline** after merge to center **`defaultBranch`** — see **`.sedea/centers/software-development/docs/development-process.md`** § *Center submodule git (two repositories)* |

**Out of scope — Sedea centers (for this rule).** Do **not** use this rule to author or maintain assets under **`.sedea/centers/<centerSlug>/`** (`rules/`, `missions/`, `skills/`, `center.yaml`). When **center** process or rules need to change, use **`improve center rules`** on the matching **`centerSlug`** — not edits driven by per-PR **`## 5. Repo rules impact`** alone.

Per-PR plan section **`## 5. Repo rules impact`** (see **development-process.md**) lists **only** **`.cursor/rules/*.mdc`** deltas for the **hosting repo** that receives the PR.

## When exploring the codebase

If you spend time reading multiple files or running searches to learn how something works (call topology, data flow, conventions, module boundaries), capture the finding as a new or updated rule in `.cursor/rules/` so future sessions start with that knowledge.

## When making changes

If your change introduces or alters architecture, conventions, call patterns, or module boundaries, update the relevant rule (or create one) before finishing the task.

Per-PR plans under the **`.sedea/operations/`** union (**`.sedea/operations/.../plans/`** or **`.sedea/operations/joint/plans/`**) carry **`## 5. Repo rules impact`** — list which **`.cursor/rules/*.mdc`** files in the **hosting repo** need a delta for that PR; keep it in sync when you touch rules outside that flow.

### Plan-anchored PRs (before commit and push)

When the work is driven by a **per-PR plan** anchored in the operations plan tree (or otherwise in scope for the active **worktree**):

1. Read that plan's **`## 5. Repo rules impact`** (or **`## N. Repo rules impact`** if the plan uses the legacy 7-section numbering).
2. For every bullet that names a path under **`.cursor/rules/*.mdc`** in the **hosting repo** (`user-auth`, `payments-web`, etc.) and calls for an **update** / **extend** / **add**, implement that **`.mdc` change in the same PR worktree** before review / rule **20** § *Commit and push cadence*, unless the bullet explicitly defers it — then **edit the plan's §5** so the deferral is honest.
3. Bullets that say **no file edit**, **verify only**, or **skip unless …** mean: confirm the code follows the existing rule; no `.mdc` diff required unless you discover drift.
4. The **`.sedea/operations/.../plans/`** tree is not the hosting repo — rule edits belong in the repo that ships the code.

See **`.sedea/centers/software-development/docs/development-process.md`** § *Per-PR plan template* item **5** — *Align hosting-repo rules before commit and push* — for the full contract.

## Format

Follow the `.mdc` frontmatter format documented in existing rules. Keep rules concise (under 50 lines when practical), one concern per file, with concrete examples where helpful.

## Git and worktree docs in `.cursor/rules/`

When a hosting-repo **`.cursor/rules/*.mdc`** file documents **git worktrees**, post-merge cleanup, or Mission Control workspace attach/detach, it **must** include **worktree removal ownership**: agents remove **only** **this pass’s** **`WORKTREE_ROOT`** when all preconditions pass — **never** repo-wide cleanup from **`git worktree list`**. Link the canonical contract: [`.sedea/centers/sedea/rules/0_hosting-repo.mdc`](.sedea/centers/sedea/rules/0_hosting-repo.mdc) § *Worktree ownership*; [`.sedea/centers/software-development/rules/20_efficient-pr-shipping.mdc`](20_efficient-pr-shipping.mdc) § *Worktree removal ownership (binding)*.

## Git and worktree docs in center rules and skills

When **any** file under **`.sedea/centers/<centerSlug>/`** (center **`rules/`**, mission **`plan.mdc`**, **`SKILL.md`**, or center **`docs/`**) mentions **`git worktree add`**, **`git worktree remove`**, **`git worktree prune`**, **`sedea_add_worktree_folder`**, **`sedea_remove_worktree_folder`**, post-merge cleanup, or **`git worktree list`**, it **must** include a **Worktree removal ownership (binding)** block (or equivalent normative one-liner): **do not remove worktrees you do not own**; **`git worktree list` is read-only** unless rule **0** § *Worktree ownership* preconditions hold for **that** path. Link rule **0** § *Worktree ownership* and, for R&D ship lanes, rule **20** § *Worktree removal ownership (binding)*. **Forbidden:** instructing agents to “clean up” or prune every worktree on the repo. **If ownership is unsure:** **stop** and use structured choice — **do not remove**, and **do not** end with prose-only idle.

## No agent gcloud secrets or env-var proposals (binding)

Agents **must not** update gcloud secrets, add environment variables to code, or **propose** new env vars. Env vars require an **explicit same-turn developer instruction** naming the variable. Normative detail: [`60_no-agent-env-secrets.mdc`](60_no-agent-env-secrets.mdc).

## Checkpoint step continuation (binding)

This file is the **hosting-repo `.cursor/rules` vs center-rules routing contract** — not a mission protocol with executable ship steps. Under **`trustLevel: checkpoint`**, turn-end modal UX for worktree-open, §5 reconciliation, ship cut-points, and post-merge cleanup lives in invoker skills (`coding-session`, `create-pr`, `promote-submodule-pin`, **`improve center rules`**) and consent gates in rules **6** / **2** / **1** / **20**. **Do not** add literal `USER_CHECKPOINT` markers here for happy-path routing-table reads, plan-anchored §5 action/verify classification, or explore-then-capture rule updates when the task already names the edit target — that would fight skill auto-advance.

| Surface | Designation | Owner |
|---------|-------------|--------|
| Consult routing table when edit layer is clear | **auto-advance** | § *Where to edit which governance (routing)* |
| Apply §5 **action** / **verify-only** / **`_None_`** bullets on plan-anchored PRs | **auto-advance** | § *Plan-anchored PRs* — hosting `.mdc` edits in same worktree |
| Explore-then-capture / change-then-update hosting `.cursor/rules` when task requires it | **auto-advance** | §§ *When exploring the codebase*, *When making changes* |
| Unclear whether change belongs in hosting `.cursor/rules` vs center rules vs sedea platform | **checkpoint** | § *Where to edit which governance* — structured choice once; no prose-only layer pick |
| Worktree ownership unsure before remove/cleanup docs apply | **checkpoint** | §§ *Git and worktree docs…* + rule **20** / sedea **0** — stop + modal; no markers in this file |
| Center / mission governance edits (`improve center rules` / `improve mission`) | **baseline-governs** (reference) | Center-maintenance / mission-maintenance missions — not this file’s edit loop |
| Turn completion after proposing a `.cursor/rules` delta in chat | **baseline-governs** (reference) | Rules **1**–**2**; pointer in Related below |

**Forbidden in this file:** prescribing prose *tell me when* / *come back when* / *I'll wait* / *over to you* / *ask how to proceed* as the primary wait surface for routing ambiguity or ownership-unsure stops; duplicating `coding-session` §5 reconciliation or rule-6 AskQuestion option tables; live `USER_CHECKPOINT` tokens on happy-path routing or §5 classification.

**Related:** Checkpoint step-table template — [`.sedea/centers/sedea/docs/pr-step-table-template.md`](.sedea/centers/sedea/docs/pr-step-table-template.md). Marker syntax — [`.sedea/centers/sedea/docs/user-checkpoint-marker-syntax.md`](.sedea/centers/sedea/docs/user-checkpoint-marker-syntax.md). Turn completion — [`.sedea/centers/sedea/rules/2_ask-question-instructions.mdc`](.sedea/centers/sedea/rules/2_ask-question-instructions.mdc); [`.sedea/centers/sedea/rules/1_agent-code-of-conduct.mdc`](.sedea/centers/sedea/rules/1_agent-code-of-conduct.mdc).
