---
description: "Ship PRs fast — parallel worktrees, one PR per worktree, single concern per PR, agent-only PR creation, never auto-PR. Loaded by a coding agent; covers commit and push cadence, worktree naming, comprehensive PR descriptions, and worktree setup conventions."
alwaysApply: false
---

# Efficient PR shipping

**On-demand rule.** Mission Control warm-up loads [`.sedea/centers/software-development/rules/bootstrap.mdc`](bootstrap.mdc) as the sole Software Development `alwaysApply: true` bootstrap. Read **this file** when you need worktrees, commit/push cadence, ship chain, and PR creation gates — not on every dispatch warm-up.

Sedea's workflow for shipping multi-PR plans fast.

## Git governance (worktree-only)

User-facing git execution for implementation and ship uses **worktrees** only — not **`git checkout -b`** on the primary hosting clone. Each PR maps to one worktree; the **worktree name** is the `--worktree-name` on center **`worktree-setup.sh`** (see [`.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc`](.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc)). **Attach order:** center **`worktree-setup.sh`** → **`sedea_add_worktree_folder`** → tracked edits; post-merge on operational hosting repos (for example **sedea-push**): **`sedea_remove_worktree_folder`** → hosting **`scripts/compose-worktree-teardown.sh`** (fail-open; agent warns on **`failed-open`** per **`.cursor/rules/dot-sedea.mdc`**) → center **`worktree-cleanup.sh`** per [`.sedea/centers/sedea/rules/0_hosting-repo.mdc`](.sedea/centers/sedea/rules/0_hosting-repo.mdc) and rule **6**. Inline sedea **`worktree-cleanup`** skill runs the same compose-teardown step — invokers need not duplicate it.

## Worktree removal ownership (binding)

**Do not remove, detach, or prune worktrees you do not own.** Canonical preconditions and forbidden patterns: [`.sedea/centers/sedea/rules/0_hosting-repo.mdc`](.sedea/centers/sedea/rules/0_hosting-repo.mdc) § *Worktree ownership*; [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc) § *Post-merge worktree cleanup*; rule **7** § *After PR merge (worktree cleanup)*.

On every Software Development ship lane (**`coding-session`**, **`plan-reconcile`** §5 fallback, Squad Leader handoff), **`git worktree remove`**, **`git worktree prune`**, and **`sedea_remove_worktree_folder`** apply **only** to **`WORKTREE_ROOT`** for **this pass** when **all** of the following hold:

| # | Precondition |
|---|--------------|
| **1** | **This pass** ran center **`worktree-setup.sh`** for that absolute path (layer 2 worktree-open on **`coding-session`**, or mission-owned git on this lane) — **or** spawn/handoff [Inherited worktree ownership](missions/plan-and-deliver/skills/coding-session/SKILL.md#inherited-worktree-ownership-upstream-handoff-binding) (`worktreeOwnership: inherited` + absolute **`worktreePath`** from the upstream creator, e.g. **`debug-and-fix`**). |
| **2** | **`WORKTREE_ROOT`** is the **exact** path from that setup step **or** the inherited **`inputs.worktreePath`** — **not** inferred from **`git worktree list`**, plan sidecar **`worktrees[]`**, or “stale” entries alone. |
| **3** | **`sedea_remove_worktree_folder`** runs **only** when **this pass** called **`sedea_add_worktree_folder`** for **that same** path — **or** inherited handoff set **`mountedViaMcp: true`** (upstream already mounted) **or** this pass attached that same inherited path once. |
| **4** | The developer confirmed **this** PR merged **or** explicitly approved removing **this** path (structured choice or clear wording in the **same** message). |

**Inherited handoff:** When precondition **1** is satisfied via inheritance, treat cleanup auto-apply the same as create-own Path A for **that path only**. **Forbidden:** treating remount-only reuse of an inherited path as “ownership unclear.”

**Forbidden (agent failure):**

- **`git worktree remove`**, **`git worktree prune`**, or **`sedea_remove_worktree_folder`** on any path that fails a precondition above.
- “Cleaning up” worktrees the developer, another Mission Control dispatch, another agent lane, or a prior session created — **`git worktree list` is read-only** unless all four preconditions pass for **that** path.
- Post-merge or “tidy up” cleanup targeting **every** worktree on the repo instead of **only** **this pass’s** **`WORKTREE_ROOT`**.
- **`git worktree remove`** on **`HOSTING_ROOT`** or any path that is not **this pass’s** **`WORKTREE_ROOT`**.
- Deleting worktree directories by hand while they remain Sedea workspace folders or while another party may still use them.

**If unsure** whether a path is yours for **this pass**, **stop** and use structured choice (`AskQuestion` / **`mission_control_present_structured_choice`**) — **do not remove**, and **do not** end with prose-only idle. Marker and option tables live on the **invoker** cleanup / ship skill — **not** as `USER_CHECKPOINT` tokens in this file. **`post-reconcile-workspace-cleanup.mjs --apply`** may remove **only** paths returned for **this plan/session** by **`detect-stale-workspaces`** after the ownership gate above — never arbitrary paths from **`git worktree list`**.

## Squad Leader on HOSTING_ROOT vs agent sessions in worktrees

There are two kinds of agent sessions:

- **Squad Leader is the main session** — stays on **`HOSTING_ROOT`** (primary hosting clone, usually on `main`), handles lightweight tasks that do not need a worktree because they do not alter tracked hosting-repo code:
  - Planning and plan updates
  - Coordinating and spawning agent sessions
- **Agent sessions** — each uses a dedicated worktree to make changes on the code:
  - Planned implementation PRs
  - PR reviews and PR comment fixes

When the Squad Leader receives an implementation request, PR review, or cross-repo task, hand off to a new agent session via the **`coding-session` protocol branch** ([`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) — same mission as `pr-review` / `create-pr`). The **`coding-session`** lane owns center **`worktree-setup.sh`** → sidecar → **`sedea_add_worktree_folder`** on the default path — **forbidden:** raw **`git worktree add`** when **`.sedea/centers/sedea/scripts/worktree-setup.sh`** exists on **`HOSTING_ROOT`**. See **`coding-session/SKILL.md`** § *Hard rules — git worktree vs workbench attach (binding)* and § *Generic flow (single repo)*.

## Hosting repo cwd for scripts (canonical)

Software Development automation scripts **`plan-state.mjs`** and **`plan-ws-completeness.mjs`** live under **`.sedea/centers/software-development/missions/plan-and-deliver/scripts/`** in the **hosting repo** whose root contains **`.sedea/`** (and usually **`.sedea/centers/sedea/`**). Canonical **`pr-review.mjs`** lives under **`.sedea/centers/sedea/scripts/`** — see § *`pr-review.mjs`* below.

**Hosting repo worktrees** (`git worktree add` for `user-auth`, `payments-web`, etc.) often have a **different** `git rev-parse --show-toplevel` and **no** `.sedea/centers/` tree. Do **not** derive script paths from the worktree root alone.

### Resolve `HOSTING_ROOT`

Before any **`plan-state.mjs`**, **`plan-ws-completeness.mjs`**, or **`pr-review.mjs`** invocation through **`.sedea/centers/sedea/scripts/run-sedea-node.sh`** (never bare **`node`**):

1. Start from the hosting repo worktree or primary hosting repo cwd.
2. Walk **up** parent directories until **`.sedea/centers/sedea/`** exists (preferred) or at minimum **`.sedea/`** at the hosting repo root.
3. Set **`HOSTING_ROOT`** to that directory (parent of `.sedea/`).
4. **`cd "$HOSTING_ROOT"`** for every script shell line; use repo-relative paths from there (for example `.sedea/centers/software-development/missions/plan-and-deliver/scripts/plan-state.mjs`).

When the agent already knows the Mission Control primary hosting repo path, set **`HOSTING_ROOT`** explicitly instead of guessing.

5. If no ancestor contains **`.sedea/`**, **stop** — use **AskQuestion** once for the hosting repo root (absolute path). Do not run **`plan-state.mjs`**, **`plan-ws-completeness.mjs`**, or **`pr-review.mjs`** from a worktree path alone.

### Node launcher (`plan-state.mjs` / `plan-ws-completeness.mjs`)

Invoke **`plan-state.mjs`** and **`plan-ws-completeness.mjs`** through **`.sedea/centers/sedea/scripts/run-sedea-node.sh`** — **never** bare **`node`**. The wrapper sources **`resolve-node.sh`** (Electron-as-Node in Sedea agent shells).

**Forbidden fallbacks when the wrapper exits non-zero:** **`brew install node`** / other package-manager Node installs; **fnm** / **nvm** bootstrap to unblock agent governance scripts; borrowing Node from another application (Codex.app, foreign Electron, etc.). **Stop and ask** the developer via structured choice — do not improvise a runtime.

### Worktree context for `resolve --cwd`

When linking a plan to a worktree, pass the **worktree** path to **`resolve --cwd`** while the shell **`cwd`** is **`HOSTING_ROOT`**:

```bash
WORKTREE_ROOT="$(pwd)"   # after cd into the hosting repo worktree
cd "$HOSTING_ROOT"

.sedea/centers/sedea/scripts/run-sedea-node.sh .sedea/centers/software-development/missions/plan-and-deliver/scripts/plan-state.mjs \
  resolve --cwd "$WORKTREE_ROOT"
```

For **`upsert-pr --repo`**, use **`$(basename "$HOSTING_ROOT")`** — the **hosting repo git remote label** (for example `app`), **not** **`$(basename "$WORKTREE_ROOT")`**. Worktree directory names (for example `app-feat-02-…`) are ephemeral paths; sidecar **`prs[].repo`** must stay stable so **`plan-state.mjs`** reconcile and post-merge **`mergedPr`** detection still work after **`worktrees[]`** is pruned.

### `pr-review.mjs`

Run **`.sedea/centers/sedea/scripts/run-sedea-node.sh .sedea/centers/sedea/scripts/pr-review.mjs`** with **`cd "$HOSTING_ROOT"`** (two-step **`PR_REVIEW_INPUT`** per [`pr-review/SKILL.md`](../missions/plan-and-deliver/skills/pr-review/SKILL.md)). **Never** bare **`node`** for **`pr-review.mjs`** — the wrapper sources **`resolve-node.sh`**.

**Forbidden fallbacks when the wrapper exits non-zero:** **`brew install node`** / other package-manager Node installs; **fnm** / **nvm** bootstrap to unblock agent scripts; borrowing Node from another application. **Stop and ask** the developer via structured choice — do not improvise a runtime.

Git / `gh` in **`pr-review`** Step 0 may use the **hosting repo worktree**; the script binary path must not assume `.sedea/` exists under the worktree root.

### Forbidden pattern (anti-example — do not copy)

```bash
ROOT="$(git rev-parse --show-toplevel)"   # worktree — often wrong for Sedea scripts
node "$ROOT/.sedea/centers/.../plan-state.mjs" ...   # forbidden: bare node + wrong root
```

Use **`HOSTING_ROOT`** plus a repo-relative script path from the hosting repo, or an absolute path to the script file under **`HOSTING_ROOT`**.

### Operations plan files (binding)

**Forbidden:** editing `.plan.md` or `.state.yaml` under **worktree** `.sedea/operations/`. Operations plan authority lives on **`HOSTING_ROOT`** only. Worktree bootstrap may copy operations for read-only context; all plan/sidecar writes target primary paths. See **`coding-session/SKILL.md`** § *Plan and sidecar IO (binding)* and **0_hosting-repo.mdc** § *Operations persistence (main hosting root only)*.

Cross-refs: [`.sedea/centers/software-development/rules/31_dispatch-scope.mdc`](31_dispatch-scope.mdc) § *Worked example*; [`.sedea/centers/sedea/rules/0_hosting-repo.mdc`](.sedea/centers/sedea/rules/0_hosting-repo.mdc).

## Plan for independence

When creating a multi-PR plan, **maximize the number of PRs that can be worked on in parallel**:

- Prefer slicing by vertical feature or module so PRs don't touch the same files.
- Explicitly mark each PR as **independent** or list its dependency (e.g. "depends on PR 2").
- Independent PRs each get a worktree from `origin/main`; a dependent PR gets a new worktree from `origin/main` only after its prerequisite merges.

## One PR per worktree, many worktrees in parallel

We work with **git worktrees** (one per PR) so we can have several PRs in flight in parallel via Mission Control and **coding-session** agent work (one session per worktree).

- Inside any single worktree, implement **at most one PR** at a time.
- Do **not** put commits for separate planned PRs in the same worktree.
- Do **not** stack worktrees or PRs unless the developer explicitly asks for it.
- When a PR is done, open it for review; the next independent PR is already being worked on in another worktree.

## Batch ship approval (Checkpoint)

When **`coding-session`** tracks **`openPrBatch.length > 1`**, one **`approve-ship-batch`** gate authorizes review → CI remediation → rebase → merge → pin promotion → worktree cleanup → **`HOSTING_ROOT`** refresh for **all** rows — per [`.sedea/centers/sedea/docs/batch-ship-checkpoint-profile.md`](.sedea/centers/sedea/docs/batch-ship-checkpoint-profile.md). **Forbidden** on the clean batch path: per-PR post-create-pr modals and per-PR **`pr-review`** disposition modals. Manual **Staging test** §7 steps remain a separate Checkpoint gate per **`coding-session`** three-stop model stop **3**.

## Review before commit

After implementing changes, **stop**. Do not `git add`, `git commit`, or `git push` until the developer explicitly confirms that we are ready to move on to the next development stage.

- Present a summary of what changed and ask for next steps using `AskQuestion`. 
- This applies to **all** workflows: feature PRs, PR comment fixes, and any other code changes.
- **Pre-review verification (hosting repo):** Before the summary and review modal / ship cut-point, complete **pre-review verification** prescribed by applicable **hosting-repo** **`.cursor/rules/*.mdc`** files. Resolve which rules apply from the PR plan **§5**, **Project rules** curation in **`coding-session`**, or rules already loaded from the worktree — per [`.sedea/centers/software-development/rules/40_maintain-rules.mdc`](40_maintain-rules.mdc). Do **not** duplicate hosting-repo commands in center rules or skills. Do not open developer review until prescribed verification passes (**exit 0**); fix failures and re-run.
- The exceptions are creating worktrees (fine without confirmation). The coding agent must not silently commit. After developer approves implementation at the ship cut-point, `coding-session` commits (with explicit consent), runs **Local test** via inline **`deploy-walk`** when applicable, then **auto-spawns** the `pre-pr-review` reviewer lane (no separate pre-PR spawn modal). After **`create-pr`**, run **Staging test** inline when §7 **`### Staging test`** has unchecked items.

**Sedea git gate (same message):** This section is a **workflow** pause — it does **not** replace [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc). Agents must satisfy **both** layers in § *Ship git consent (workflow + Sedea gate)* below.

## Ship git consent (workflow + Sedea gate)

Two **independent** layers apply on **implementation** lanes (`coding-session` and ship children). Satisfy **both**; neither substitutes for the other.

| Layer | Source | Controls | User grants via |
|-------|--------|----------|-----------------|
| **1 — Workflow** | § *Review before commit* (above) | Pause before advancing ship stage (summary, next skill, cut point) | **AskQuestion** — workflow-only options (*more changes*, *defer*, …) at cut-point; **auto-advance** after cut-point for pre-PR spawn, clean-go create-PR, and post-merge cleanup |
| **2 — Git history** | [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc) | `git commit`, `git push`, PR steps that imply push | **Same user message** includes *commit* / *push*, **or** an **AskQuestion** pick on **that turn** whose `id` is `commit-only`, `executive-override-push`, or `commit-push-pr` (rule 6 § *After the user is satisfied*) — on **`coding-session`**, **`git push`** before **`pre-pr-review`** **`go`** requires **`executive-override-push`** or explicit same-message *push* per [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) § *Pre-PR ship gate (push/PR)* |

**Cross-turn rule:** A workflow **AskQuestion** pick in a **prior** turn does **not** authorize commit or push. Ship cut-point approval authorizes commit + Local test + **auto** pre-PR spawn on the **next** turn — it does **not** require a separate *run pre-pr-review* pick.

**When both layers apply in one turn**, use **one AskQuestion** with separate workflow options and git options (brief `label` text per [`.sedea/centers/sedea/rules/2_ask-question-instructions.mdc`](.sedea/centers/sedea/rules/2_ask-question-instructions.mdc)), **or** split turns: workflow **AskQuestion** first, then a **new** user message with *commit* / *push*.

| Developer need | Example workflow `options` | Example git `options` (layer 2 — same AskQuestion when committing this turn) |
|----------------|----------------------------|-------------------------------------------------------------------------------|
| Review only, no git yet | *More changes* · *Defer ship* · More details for option _ | Omit git options |
| Commit at cut-point (no push yet) | *More changes* · *Defer ship* · More details for option _ | *Commit only* · *No git actions* · More details for option _ |
| Push before pre-PR **`go`** | — | **Forbidden** in modals unless developer named **executive override** → **`executive-override-push`** on cut-point only |

Planning-tree picks use [`.sedea/centers/software-development/rules/30_planning-target-resolution.mdc`](30_planning-target-resolution.mdc) § *Sedea input channel* — not this table.

### AskQuestion vs git commit/push (same message)

| Developer signal (this turn or **AskQuestion** pick) | May continue workflow / spawn next skill? | May run **`git commit`**? | May run **`git push`**? |
|------------------------------------------------------|----------------------------------------|---------------------------|-------------------------|
| *Proceed*, *next stage*, *looks good*, *approved cut point* (workflow only) | **Yes** — advance to commit + Local test on pick turn | **No** — unless the **same user message** also says *commit* (or equivalent) | **No** — unless the **same message** says *push* or *commit and push* |
| *Committed* / *ready for pre-PR review* (after implementation approval + commit + Local test when applicable) | **Yes** — **auto-spawn **`pre-pr-review`** per **`coding-session`** (no spawn modal) | **No** — unless the **same message** authorizes a **new** *commit* | **No** — until the **same message** says *push* |
| *commit*, *land the worktree*, *create a commit* in **same user message** | **Yes** | **Yes** (if there are changes to commit; skip if tree clean per cadence) | **No** — unless the message also says *push* |
| *push*, *commit and push*, *publish the worktree* in **same user message** | **Yes** | **Yes** when commit is included or already done | **Yes** |

If **AskQuestion** approved a workflow step in a **prior** turn, wait for a **new** user message containing *commit* / *push* before running git write commands. Clean **`pre-pr-review`** **`go`** authorizes inline **`create-pr`** on the **next** turn without a Create-PR modal — **not** commit or push consent.

### Mission-owned git when `plan and deliver` is active

When Mission Control dispatches **`plan and deliver`** on **software-development**, commit/push cut points in **this rule** and in **`coding-session`**, **`pre-pr-review`**, and **`create-pr`** count as **mission-owned git** per [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc) § *Mission-owned git*.

| Situation | [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc) § *Hosting-repo file edits* |
|-----------|-------------------------------------------------------------------------------------------------------------------------------------|
| **`coding-session`** / ship child on hosting repo worktree | **Do not** duplicate rule-6 pre-edit or post-satisfaction AskQuestion for the same commit/push — follow **this rule** § *Review before commit* and the skill cadence |
| **`plan and deliver`** Squad Leader (planning only) | Usually no hosting-repo writes — rule 6 often N/A on that lane |
| Edits under **`.sedea/centers/`** on a **center-maintenance** or hosting dispatch | Rule 6 **applies** (not mission-owned) unless another mission owns git for that lane |

**Both layers still apply** on implementation lanes — see § *Ship git consent (workflow + Sedea gate)*. Ship cut-point approval is **not** commit or push consent by itself on the modal turn — **Act** runs on the response turn. Approving cut-point **does** authorize **auto** pre-PR spawn after commit + Local test complete.

## deploy-walk vs plan-reconcile (not chained)

Finishing **`deploy-walk`** (checklist complete, capstone todo **`deploy-test-plan-verified`** → `done`) does **not** start **`plan-reconcile`**.

| Skill | Ends when | Does **not** auto-start |
|-------|-----------|-------------------------|
| **`deploy-walk`** | Deploy §7 walked; Status `done` | **`plan-reconcile`** |
| **`plan-reconcile`** | Archive / follow-ups triage per **`plan-state.mjs`** | (nothing upstream) |

Start **`plan-reconcile`** separately: developer phrase on active **`coding-session`**, or **`coding-session`** [Plan-reconcile handoff (inline)](../missions/plan-and-deliver/skills/coding-session/SKILL.md#plan-reconcile-handoff-inline) after production walk when the developer **explicitly** chooses reconcile — see **`plan-reconcile/SKILL.md`** § *When this skill runs* and **development-process.md** § *Ship chain*.

## Pre-PR review — spawn-only from **`coding-session`**

**Rule (read first).** The **only** path that may execute [`pre-pr-review/SKILL.md`](../missions/plan-and-deliver/skills/pre-pr-review/SKILL.md) is a **fresh spawned child lane** opened by active **`coding-session`** via MCP **`mission_control_spawn_agent`** after ship cut-point + Local test (or documented skip). **Auto-spawn** on **`coding-session`** means **call MCP spawn + wait** for child **`mission_control_send_agent_result`** — **not** load and run review steps inline on the coding-session lane.

| Lane | May run **`pre-pr-review`**? | Handoff |
|------|------------------------------|---------|
| **`coding-session`** | **Spawn only** — **forbidden** inline | Call **`mission_control_spawn_agent`** per [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) § *Pre-PR review handoff* |
| **`pre-pr-review`** child lane | **Yes** — sole execution surface | Fresh reviewer lane; terminal **`mission_control_send_agent_result`** |
| **Other lanes** (Squad Leader, planning, **`pr-review`**, …) | **No** | Direct developer to **`coding-session`** ship chain |

When the user says *run pre-PR review* or *pre-pr-review* on **`coding-session`**, interpret it as **spawn the reviewer child lane** — **not** permission to execute the skill body inline on this lane.

## PR creation — inline **`create-pr`** on **`coding-session`**

**Rule (read first).** The **only** path that may call `gh pr create` (or any equivalent GitHub API/MCP call that creates, drafts, or reopens a PR) is the active **`coding-session`** agent **executing [`create-pr/SKILL.md`](../missions/plan-and-deliver/skills/create-pr/SKILL.md) inline** on the **inline GitHub** route after **`pre-pr-review`** returns `go` and that skill's gates pass. Clean **`go`** runs inline **`create-pr`** **automatically** on the next turn ([Inline create-pr (auto on clean go)](../missions/plan-and-deliver/skills/coding-session/SKILL.md#inline-create-pr-auto-on-clean-go)); exceptional follow-up append uses [Create-PR handoff after go](../missions/plan-and-deliver/skills/coding-session/SKILL.md#create-pr-handoff-after-go).

### Outsider repos (tapcart product submodules)

**`create-pr`** classifies **outsider repos** before any GitHub API call. When the worktree targets **`tapcart-push`** or **`tapcart-merchant-dashboard`** (see **`.cursor/rules/push-monorepo-submodules.mdc`**), Sedea agents **must not** call `gh pr create`. Instead, **`create-pr`** emits an **outsider PR handoff prompt** for an external agent outside Mission Control **after** verifying the branch exists on `origin`; the developer pastes it and resumes **`coding-session`** when the PR URL is known. See **`create-pr/SKILL.md`** § *PR route evaluation*, § *Remote branch gate*, and § *Outsider PR handoff prompt*.

**Remote branch before outsider handoff:** On outsider repos, **`create-pr`** runs `git ls-remote --heads origin <worktreeName>` before emitting the outsider prompt. If empty, it blocks with **`blockedReason: remote-branch-missing`** and offers a push modal — **do not** emit the handoff prompt. **`commit-only`** at [Ship cut-point gate](../missions/plan-and-deliver/skills/coding-session/SKILL.md#ship-cut-point-gate-approve-commit-local-test) leaves the branch **local-only** by design; outsider handoff requires an explicit push (`commit-push` at cut-point, or **`push-branch-now`** when blocked).

| Lane | Outsider repo | May call `gh pr create`? | Handoff |
|------|---------------|--------------------------|---------|
| **`coding-session`** running **`create-pr` inline** | **Yes** (`tapcart-push`, `tapcart-merchant-dashboard`) | **No** | Outsider-handoff prompt only; [Post-outsider-handoff gate](../missions/plan-and-deliver/skills/coding-session/SKILL.md#post-outsider-handoff-gate) |
| **`coding-session`** running **`create-pr` inline** | **No** (other repos) | **Yes** when inline route + gates pass | `gh pr create` when authorized |

**Outsider create vs merge:** **Outsider** applies to **`gh pr create`** on tapcart product submodules (handoff above). **Approve and merge** after clean **`pr-review`** uses the agent-delegated path in § *PR merge* — not manual developer merge on GitHub.

| Lane | May call `gh pr create`? | Handoff |
|------|--------------------------|---------|
| **`coding-session`** running **`create-pr` inline** | **Yes** — sole supported opener | Load **`create-pr/SKILL.md`** on this lane after clean **`go`** auto path or exceptional Create-PR gate |
| **`coding-session`** (other steps) | **No** | Complete ship order through cut-point → Local test → **spawn** **`pre-pr-review`** (forbidden inline) → inline **`create-pr`** |
| **Other lanes** (Squad Leader, planning, `pre-pr-review`, `pr-review`, `master-planner`, …) | **No** | Prepare worktree/context; direct developer to **`coding-session`** ship chain or emit copy-paste prompt — never open from the current lane |

**All other lanes:** do **not** call `gh pr create` except **`coding-session`** while executing inline **`create-pr`** on the **inline GitHub** route per the table above.

When the user or task says "open a PR", "push and open a PR", or "create a pull request" on a **non–`coding-session`** lane, interpret it as **hand off to the `coding-session` ship chain** — not as permission to run `gh pr create` on this lane.

## PR merge — inline **`coding-session`** after clean **`pr-review`**

**Rule.** The **only** path that may call `gh pr merge` or `gh pr review --approve` (or equivalent GitHub merge/approve API) is the active **`coding-session`** agent after inline **`pr-review`** is clean and merge preconditions hold:

| Repo class | Merge path |
|------------|------------|
| **Non-outsider** (hosting root, own-center repos) | Developer selects **`approve-merge`** at [Post-pr-review merge approval gate](../missions/plan-and-deliver/skills/coding-session/SKILL.md#post-pr-review-merge-approval-gate) → [Act after merge approval](../missions/plan-and-deliver/skills/coding-session/SKILL.md#act-after-merge-approval) |
| **Outsider repos** (`tapcart-push`, `tapcart-merchant-dashboard`) | Developer picks **`start-pr-review-delegate-merge`** at [Post-create-pr handoff gate](../missions/plan-and-deliver/skills/coding-session/SKILL.md#post-create-pr-handoff-gate) (or same-message delegation); when **`mergeDelegationReady`**, [Pre-merge authorization gate](../missions/plan-and-deliver/skills/coding-session/SKILL.md#pre-merge-authorization-gate) → **`delegate-merge-confirm`** → [Agent-delegated PR approve and merge](../missions/plan-and-deliver/skills/coding-session/SKILL.md#agent-delegated-pr-approve-and-merge) |

**Forbidden:** `gh pr merge` or `gh pr review --approve` without structured choice at the applicable merge gate (`approve-merge`, **`delegate-merge-confirm`**, or equivalent); prose handoffs that require the developer to click merge on GitHub when delegation was authorized and preconditions pass; **`gh pr create`** on outsider repos from any lane.

See [`pr-review/SKILL.md`](../missions/plan-and-deliver/skills/pr-review/SKILL.md) § *PR review cycle (normative)* and [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) § *PR review cycle (after PR open)*.

### User phrases → required handoff (not `gh pr create`)

| User says (paraphrase OK) | Current lane | Required action |
|---------------------------|--------------|-----------------|
| *Open a PR* / *create the pull request* | **`coding-session`** before **`pre-pr-review`** **go** | **Do not** call `gh pr create`. Explain ship order: implementation review → cut-point (approve, commit, Local test **`deploy-walk`**) → auto **`pre-pr-review`** → inline **`create-pr`** on clean **go**. |
| *Open a PR* / *create the pull request* | **`coding-session`** after **`pre-pr-review`** clean **`go`** | Run inline **`create-pr`** automatically per § *Inline create-pr (auto on clean go)* in [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) — `gh pr create` only inside that procedure when gates pass. |
| *Open a PR* | Planning, Squad Leader, **`pre-pr-review`**, **`pr-review`**, **`master-planner`**, … | Prepare context; direct developer to **`coding-session`** ship chain — **never** `gh pr create` here. |
| *Push and open a PR* | **`coding-session`** | Run § *Commit and push cadence* (same-message *commit* / *push*) **first** when changes exist; then pre-PR / inline **`create-pr`** handoff — not one combined `gh pr create` step outside the inline procedure. |

This rule covers **chat output**, not just tool calls. After `git push`, do **not** surface or paraphrase GitHub's remote hint (the `Create a pull request for '<branch>' on GitHub by visiting: https://github.com/<org>/<repo>/pull/new/<branch>` line). That URL is the GitHub UI form and bypasses the protocol. The only PR-creation artifacts you present from other lanes are direction to **`coding-session`** or a copy-paste prompt for a future ship pass.

## Worktree setup in plans

Every multi-PR plan must end with a **Worktree setup** section containing copy-pasteable commands. Resolve **`WORKTREE_ROOT`** per [`.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc`](.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc) § *Worktree directory path (hosting repo)* — **do not substitute** paths directly under `dirname(HOSTING_ROOT)/` outside `{repo-basename}-worktrees/`.

**Path formula is normative in rule 7 § *Worktree directory path (hosting repo)* — do not substitute.**

**Normative path (Mission Control and `coding-session`):** center **`worktree-setup.sh`** from **`HOSTING_ROOT`**, then MCP attach when the setup JSON hint reports **`nextAction: attach-required`**. **Forbidden on the default path:** inline **`git worktree add`**, **`sedea_add_worktree_folder`** before setup exits **0**, and default-path inline **`worktree-bootstrap`**. See [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) § *Generic flow (single repo)* and § *Center worktree scripts (binding)*.

Canonical form for creating a worktree with a new worktree name:

```bash
HOSTING_ROOT="<absolute-hosting-root>"
REPO_BASENAME="$(basename "$HOSTING_ROOT")"
WORKTREES_DIR="$(dirname "$HOSTING_ROOT")/${REPO_BASENAME}-worktrees"
WORKTREE_NAME="<worktree-name>"           # rule 7 / this rule § *Worktree naming*
WORKTREE_DIR="${REPO_BASENAME}-$(echo "$WORKTREE_NAME" | tr '/' '-')"
WORKTREE_ROOT="${WORKTREES_DIR}/${WORKTREE_DIR}"
mkdir -p "$WORKTREES_DIR"
BASE_REF="origin/main"

"$HOSTING_ROOT/.sedea/centers/sedea/scripts/worktree-setup.sh" \
  --hosting-root "$HOSTING_ROOT" \
  --worktree-path "$WORKTREE_ROOT" \
  --worktree-name "$WORKTREE_NAME" \
  --base-ref "$BASE_REF"
# Parse stdout JSON — bootstrapStatus success-class before implementation
# Mission Control: sedea_add_worktree_folder — absolute path to WORKTREE_ROOT when nextAction: attach-required
```

**Attach the worktree in Sedea.** In Mission Control chat, invoke MCP **`sedea_add_worktree_folder` only** with JSON `{ "path": "<absolute-worktree-root>" }` (optional `"name"` for the explorer label). Use an **absolute** `path`. **Forbidden:** editor **Add Folder to Workspace**, hand-edited **`.code-workspace`** attach, or calling **`sedea_add_worktree_folder`** **before** **`worktree-setup.sh`** exits **0**.

**Lane ownership:** On a spawned **`coding-session`** implementation lane, [Generic flow](../missions/plan-and-deliver/skills/coding-session/SKILL.md) steps 1–4 are normative. Set **`outputs.bootstrapStatus: success`** only after step **1** (**`success`** / **`skipped-idempotent`**) or mandatory step **4** post-setup script when hint is **`skipped-noop`** on **`mode: none`** hosts. **Do not** run inline **`worktree-bootstrap`** after the default path completes. Retry only per **`coding-session/SKILL.md`** § *Worktree bootstrap (inline mandatory)* when setup or step **4** failed.

**Two-phase bootstrap (sedea-push and `mode: none` script-bootstrap hosts).**

| Phase | Mechanism | Dev-ready? |
|-------|-----------|------------|
| **1 — Center setup** | **`.sedea/centers/sedea/scripts/worktree-setup.sh`** on **`HOSTING_ROOT`** | Creates worktree; may return **`bootstrapStatus: skipped-noop`** |
| **2 — Hosting script** | **`./scripts/bootstrap-worktree-dev.sh "$WORKTREE_ROOT"`** from primary **`HOSTING_ROOT`** after MCP attach | **Required** before implementation when phase 1 hint is **`skipped-noop`** and dot-sedea **`worktreeBootstrap.mode: none`** |

**`skipped-noop` ≠ dev-ready** on script-bootstrap hosts. Center setup JSON hint alone must not set **`outputs.bootstrapStatus: success`** without phase **2** when phase **1** returned **`skipped-noop`**.

**Bootstrap inside setup (default).** Center **`worktree-setup.sh`** runs overlay bootstrap per **`.cursor/rules/dot-sedea.mdc`** § *Worktree bootstrap mode* (`extensions-only-link`, `none`, or hosting overlay). **`coding-session`** sets **`outputs.bootstrapStatus: success`** from setup hints when **`success`** or **`skipped-idempotent`**, or after Generic flow step **4** when **`skipped-noop`**. On setup failure, ship phase stays at `worktree` until retry succeeds.

| Profile | When | Action |
|---------|------|--------|
| **Center setup (default)** | **`.sedea/centers/sedea/scripts/worktree-setup.sh`** on **`HOSTING_ROOT`** | Setup creates worktree + fast bootstrap; parse JSON hints |
| **Inline retry (exception)** | Setup failed or documented break-glass | **`worktree-bootstrap`** inline only — see deprecated skill + **`coding-session`** retry section |

**Push order (all profiles):** [`.cursor/rules/dot-sedea.mdc`](.cursor/rules/dot-sedea.mdc) § *Push before worktrees (mandatory)* and § *Submodule pins* when present; after center **`defaultBranch`** merges, § *Center submodule pin promotion (hosting repo)* below. Submodule init in a worktree uses the **committed** gitlink on that worktree's base ref — not an uncommitted pointer on the primary clone.

#### Hosting overlay reference (retry / legacy — not default path)

When center setup is unavailable or a documented retry invokes **`worktree-bootstrap`**, read [`.cursor/rules/dot-sedea.mdc`](.cursor/rules/dot-sedea.mdc) § *Worktree bootstrap mode* and **`./scripts/bootstrap-worktree-dev.sh --help`** on **`HOSTING_ROOT`**. **`full`** bootstrap escalation on the center setup path is **forbidden** (exit **11** warm-primary, exit **12** overlay missing).

**Detach merged worktrees from the workbench.** After a PR merges, on the **`coding-session`** lane run [Post-merge workspace cleanup](../missions/plan-and-deliver/skills/coding-session/SKILL.md#post-merge-workspace-cleanup) **before** Production `deploy-walk` — **only** for **this pass’s** **`WORKTREE_ROOT`** when § *Worktree removal ownership (binding)* preconditions all pass. Invoke **`sedea_remove_worktree_folder`** first, then hosting **`"$HOSTING_ROOT/scripts/compose-worktree-teardown.sh" --worktree-path "$WORKTREE_ROOT"`** (parse stdout JSON; **warn the developer** when **`composeTeardownStatus`** is **`failed-open`** per **`.cursor/rules/dot-sedea.mdc`** § *Post-merge compose teardown*), then center **`worktree-cleanup.sh`** with ownership attestation — **forbidden:** inline **`git worktree remove`** + **`git pull origin main`** on the default path when the center script applies.

Use **`origin/main`** as **`--base-ref`**, **not** local **`main`** — local **`main`** checked out elsewhere breaks naive worktree creation.

- For each **independent** PR, emit the **`worktree-setup.sh`** block above (or one command per worktree). These can be created and worked on immediately.
- For each **dependent** PR, emit the command but comment that it should be created only after the predecessor merges.
- Include cleanup for after PRs merge: for **this session’s** retired **`WORKTREE_ROOT` only** (§ *Worktree removal ownership (binding)*), **`sedea_remove_worktree_folder`** with that worktree's **absolute** path (Mission Control first), hosting **`compose-worktree-teardown.sh`** (fail-open; warn on **`failed-open`**), then **`worktree-cleanup.sh`** for **that path only** — **never** repo-wide **`git worktree prune`** or removal of other agents’ worktrees.
- In the **Worktree setup** narrative, after setup succeeds, tell the agent to invoke **`sedea_add_worktree_folder`** with that worktree's **absolute** path when **`nextAction: attach-required`**.

### Dirty hosting repo before center setup

[`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) § *Generic flow* runs a **dirty-tree gate** inside center **`worktree-setup.sh`** (exit **10**). On **operational hosting repos** (for example **sedea-push**), **`.cursor/rules/dot-sedea.mdc`** § *Operational repo — dirty tree and worktrees* and § *Housekeeping pass (dirty hosting tree before bootstrap)* override generic blocking: commit + push + promote center pin when needed, then retry setup — do not refuse worktrees solely for submodule gitlink drift or documented submodule porcelain once upstream center scripts align. When dot-sedea does **not** declare an operational exception, on hosting repos that pin `.sedea/` via git submodules (**.cursor/rules/dot-sedea.mdc** § *Submodule pins* on the active hosting repo), **submodule gitlink-only** porcelain under `.sedea/centers/` and `.sedea/operations/` does **not** block worktrees when `git diff --stat -- <submodule-path>` shows pointer-only drift (2 +/- per submodule). All other non-empty porcelain still blocks.

## Center submodule pin promotion (hosting repo)

When changes under a **non-built-in** center submodule (for example **`.sedea/centers/software-development/`**) merge to that center's **`defaultBranch`**, agents **must** run [`.sedea/centers/sedea/skills/promote-submodule-pin/SKILL.md`](.sedea/centers/sedea/skills/promote-submodule-pin/SKILL.md) **inline** on the hosting-repo lane so **`origin/main`** records the new gitlink. Entry routing: [`.sedea/centers/sedea/rules/0_hosting-repo.mdc`](.sedea/centers/sedea/rules/0_hosting-repo.mdc) § *Pin promotion routing* — **direct** invoke, or **cleanup-hint** only when post-merge **`worktree-cleanup.sh`** reports drift (`nextAction: promote-pin-required` with **`centerPinDriftPaths`**; host background when **`autoPromoteSubmodulePins: true`**). Local pointer drift on the primary clone is **not** enough for worktree bootstrap — see § *Dirty hosting repo before center setup* and [`.sedea/centers/software-development/docs/development-process.md`](../docs/development-process.md) § *Center submodule git (two repositories)*.

Pin the center **`defaultBranch`** tip only. Git is **skill-owned** per [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc) § *Submodule pin promotion (skill-owned)*. Canonical semantics: [`.sedea/centers/sedea/rules/3_center.mdc`](.sedea/centers/sedea/rules/3_center.mdc) § *Git repo semantics*.

**Mandatory before ship row close (binding):** When the ship chain touched submodule gitlinks, **`prShipComplete: true`** is **forbidden** until hosting **`origin/main`** records merged tips and **`HOSTING_ROOT`** is refreshed. Pin promotion is **not** optional developer follow-up — see **`coding-session/SKILL.md`** § *Hosting-pin-complete gate (before prShipComplete)*. **Forbidden recap language:** *"if you want"*, *"optional follow-up"*, *"when you're ready"*, *"remains follow-up"*.

**Calibration:** `incident_hosting_pin_promotion_treated_optional_2026-08-03.agent-incident-report.md`.

**Launch from worktree (after bootstrap success — when hosting repo documents dev launcher):**

When dot-sedea documents a dev launcher script on the hosting repo:

```bash
cd "$WORKTREE_ROOT"
# Run the launcher and fast-restart variants per dot-sedea § *Dev launcher*
```

Hub activation must reference add-on packages under **`WORKTREE_ROOT`**, not the primary clone path. **Omit this block** on hosting repos without a documented launcher.

**Agent verification (minimum before ship chain):** **`bootstrapStatus: success`**; then complete checks in dot-sedea § *Fast bootstrap verification checklist* when present. Verification is **hosting-repo specific** — do not assume one smoke model for every script repo:

| dot-sedea mode | Typical verification (examples — dot-sedea wins when present) |
|----------------|------------------------------------------------------------------|
| **`script-bootstrap`** | Script exit **0**; phases in that repo's checklist (for example Docker, submodules, deps, local-docker on orchestrator repos) |
| **`full`** / linked-build | Build artifacts and add-on **`node_modules`** per dot-sedea checklist; optional fast-restart launch when documented |
| **`submodule-init`** | Submodule checkout + attested skip flags — not full compile smoke |

## Commit and push cadence

How to run the full cadence:

### `plan-state.mjs`

Run all **`plan-state.mjs`** invocations per § *Hosting repo cwd for scripts (canonical)*. On Mission Control lanes, prefer explicit plan paths from spawn **`inputs`** or **`--file`** / **`--slug`** flags — see [`.sedea/centers/software-development/rules/31_dispatch-scope.mdc`](31_dispatch-scope.mdc) § *Plans and docs paths*.

### User input

Per **[`30_planning-target-resolution.mdc`](30_planning-target-resolution.mdc)** § *Planning vs ship cadence*:

- **PR comment triage** — **`pr-review`** ([`pr-review/SKILL.md`](../missions/plan-and-deliver/skills/pr-review/SKILL.md)) via **AskQuestion**, natural language (*triage PR comments*, *run pr-review*), or **coding-session** when PR context is already loaded.
- **Commit and push** — **AskQuestion** or natural language in the **same user message** that requests *commit* and/or *push* (per **[`6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc)**), e.g. *commit and push*, *land the worktree*.

### Steps

Apply § *Hosting repo cwd for scripts (canonical)* for every script invocation in this cadence (`HOSTING_ROOT`, `WORKTREE_ROOT`).

1. **Commit** (step 1). **Skip this step if `git status --short` is empty** — typical when the worktree is already committed after **`pre-pr-review`** **go**. Do not amend the previous commit and do not pass `--allow-empty`; just proceed to step 2.
2. Push to remote.
3. **PR comment reconciliation (`pr-review` → this cadence).** This step is **only** [`pr-review`](../missions/plan-and-deliver/skills/pr-review/SKILL.md) **Step 5 — GitHub only** (not Steps 1–4). When this coding session already ran that skill’s Steps 1–4 (per-comment dispositions in the Step 4 report) and **GitHub** reply / resolve / minimize / summary from Step 5 has **not** run yet, the **commit and push cadence is incomplete** until Step 5 runs — **same agent turn**, immediately after the push succeeds (so the summary can cite `git rev-parse --short HEAD` in the **worktree**). Execute from **`HOSTING_ROOT`**: `cd "$HOSTING_ROOT" && .sedea/centers/sedea/scripts/run-sedea-node.sh .sedea/centers/sedea/scripts/pr-review.mjs` (two-step `PR_REVIEW_INPUT` per that skill). If thread IDs or PR identity are missing from context, re-run that skill’s **Step 1** script array for the same PR, then Step 5. If there was no `pr-review` triage in this chat, **skip step 3 entirely** — do not run the script.

   **Anti-patterns (agent failure):**

   | Symptom | Required response |
   |---------|-------------------|
   | Push succeeded, **`pr-review`** Steps 1–4 ran in this chat, no **`pr-review.mjs`** Step 5 invocation | Run Step 5 **before ending the turn** — cadence incomplete |
   | **`CHANGES_REQUESTED`** unchanged, assistant turn ends at push recap | Same — see **`pr-review`** § *Step 5 turn invariant* |
   | Post-fix modal offers merge / full re-triage without Step 5 | **Forbidden** — [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) § *Inline PR review after PR creation* |

   **Forbidden:** ending the assistant turn at push when Step 4 ran in-session; opening post-create-pr or pre-merge modals before **`outputs.githubReconciliationStatus: complete`** when Step 5 was required.

4. **Link the PR to its plan sidecar.** After push (and after step 3 when applicable), attempt to upsert the PR number into the plan sidecar so `plan-reconcile` can later archive the plan when all linked PRs merge. Run with **`cd "$HOSTING_ROOT"`** (`WORKTREE_ROOT` = hosting repo worktree where commit/push ran):

   ```bash
   WORKTREE_ROOT="$(pwd)"   # hosting repo worktree (before cd to HOSTING_ROOT)
   cd "$HOSTING_ROOT"
   
   # Which plan owns this worktree? (exit 2 = none; skip silently)
   .sedea/centers/sedea/scripts/run-sedea-node.sh .sedea/centers/software-development/missions/plan-and-deliver/scripts/plan-state.mjs \
  resolve --cwd "$WORKTREE_ROOT"
   # → prints "<slug>\t<planPath>" on exit 0.

   # Does an open PR already exist for this worktree? (--head = worktree name from worktree-setup.sh --worktree-name)
   WORKTREE_NAME="<worktree-name>"   # same string as --worktree-name on worktree-setup.sh; from plan Worktree setup or sedea rule 7
   gh -C "$WORKTREE_ROOT" pr list --head "$WORKTREE_NAME" --json number --jq '.[0].number // empty'
   # → prints a number on stdout, or nothing (first push, a PR-creating agent not yet invoked).

   # If both resolved: upsert into sidecar prs[] (idempotent).
   .sedea/centers/sedea/scripts/run-sedea-node.sh .sedea/centers/software-development/missions/plan-and-deliver/scripts/plan-state.mjs \
  upsert-pr \
     --slug <slug-from-resolve> \
     --repo "$(basename "$HOSTING_ROOT")" \
     --number <number-from-gh>
   ```

   Skip silently when `resolve` exits non-zero (session with no plan) or `gh pr list` returns empty (first push — the PR will appear after inline **`create-pr`** on **`coding-session`**; the **next** commit-and-push pass picks it up). Never block this cadence on a helper failure — log and continue with the remaining steps. **Exit code 3** from `upsert-pr` means the resolved plan is not a PR plan (the helper refuses to link PRs onto Master / Phase / top-level topic / stub plans, since sidecar `prs[]` is reconcile-tracked); surface the helper's stderr message verbatim so the user can fix the worktree → plan linkage, then continue with the remaining steps.

   The [`pr-review` protocol branch](../missions/plan-and-deliver/skills/pr-review/SKILL.md) runs the same `upsert-pr` call at its Step 0 (after the PR is resolved) as a belt-and-braces linker for cases where no follow-up commit-and-push pass ever happens — `pr-review` triage that ends with all comments skipped (no commit, no push), quiet PRs with zero reviewer rounds, or single-commit PRs merged straight from GitHub. **`pr-review`** and this cadence upserts are idempotent against each other.
5. Check if this is the **first push** for the worktree (e.g. `git push` output contains `[new branch]` for the worktree ref, or `gh pr list` in step 4 returned empty). If so, **`create-pr`** handoff is mandatory — **not** a modal option before **`pre-pr-review`** **`go`**. The handoff **replaces** any GitHub create-PR URL.
   - **Active `coding-session` lane** — follow [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md) § *Pre-PR ship gate (push/PR)*: **no** push or create-PR modal options until **`prePrReviewCleared`**. First push + inline **`create-pr`** run automatically after **`pre-pr-review`** clean **`go`** per § *Inline create-pr (auto on clean go)* — not from cut-point or implementation continuation modals. If pre-PR review has not run yet, do not skip to prompt-only PR creation; run cut-point and **`pre-pr-review`** first.
   - **Other lanes** — generate a copy-paste PR prompt per **`create-pr/SKILL.md`** § *Outsider PR handoff prompt* or § *Prompt fallback*, or direct the developer to **`coding-session`** ship chain.
   On subsequent pushes, only commit/push/reconcile/upsert — skip **`create-pr`** handoff until a new first-push worktree needs a PR.

6. **Leader §8 host sync (detached lanes).** This cadence usually runs on **`coding-session`** or another **detached** lane — not on the **plan and deliver** Squad Leader dispatch. The leader §8 table does **not** auto-update from commit/push, `upsert-pr`, or **`pr-review`** reconciliation on this lane alone.

   After steps 1–5 finish successfully, ensure the **`coding-session`** terminal (or next re-emit) includes §8 **`outputs`** (`targetPlanPath`, `shipPhase`, `rowStatus`, …) so Mission Control host sync updates the leader dispatch (**`plan.mdc`** §8; **`development-process.md`** § *Leader-lane §8 host sync*). **Forbidden:** nudging manual **Ship recap** paste on the leader dispatch.

   **Silence on a detached lane is not ship complete.** If the **plan and deliver** dispatch stays active, missing host sync means §8 rows stay stale — do not propose dispatch **`resolved`** from the leader lane until host-sync updates or explicit planning-only choice (see **`plan.mdc`** §8 *Pre-resolution checklist*).

   Suggested `shipPhase` from this pass (when `resolve` returned a PR plan) — include on **`coding-session`** terminal re-emit:

   | Cadence outcome | Suggest `shipPhase` |
   |-----------------|---------------------|
   | Push only (no **`pr-review`** step 3/5 in this turn) | `implementing` |
   | Step 3 / Step 5 GitHub reconciliation ran | `pr-review` |
   | Step 4 linked an open PR (`upsert-pr` succeeded) | at least `pr-open` if the PR already exists |

   Include `targetPlanPath` from **`resolve`**, `rowStatus: open` while work continues, and `remainingTasks` when known.

**Examples — pick the block that matches the hosting repo.** Do not mix patterns on one repo.

Example A — **hosting repo** (root contains `.sedea/centers/sedea/`; stacked PRs on `main` per [`.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc`](.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc)):

```
## Worktree setup

From the hosting repo root (directory containing `.sedea/`):

HOSTING_ROOT="$(pwd)"
REPO_BASENAME="$(basename "$HOSTING_ROOT")"
WORKTREES_DIR="$(dirname "$HOSTING_ROOT")/${REPO_BASENAME}-worktrees"
BASE_REF="origin/main"
mkdir -p "$WORKTREES_DIR"

# Independent — start immediately (stack index + slug; default prefix feat)
WORKTREE_NAME="feat/01-token-auth"
WORKTREE_DIR="${REPO_BASENAME}-$(echo "$WORKTREE_NAME" | tr '/' '-')"
WORKTREE_ROOT="${WORKTREES_DIR}/${WORKTREE_DIR}"
"$HOSTING_ROOT/.sedea/centers/sedea/scripts/worktree-setup.sh" \
  --hosting-root "$HOSTING_ROOT" \
  --worktree-path "$WORKTREE_ROOT" \
  --worktree-name "$WORKTREE_NAME" \
  --base-ref "$BASE_REF"
# Mission Control: sedea_add_worktree_folder — absolute path when attach-required

WORKTREE_NAME="improve/02-logger"
WORKTREE_DIR="${REPO_BASENAME}-$(echo "$WORKTREE_NAME" | tr '/' '-')"
WORKTREE_ROOT="${WORKTREES_DIR}/${WORKTREE_DIR}"
"$HOSTING_ROOT/.sedea/centers/sedea/scripts/worktree-setup.sh" \
  --hosting-root "$HOSTING_ROOT" \
  --worktree-path "$WORKTREE_ROOT" \
  --worktree-name "$WORKTREE_NAME" \
  --base-ref "$BASE_REF"

# Depends on PR 1 — after feat/01-token-auth merges
# repeat worktree-setup.sh for feat/03-session-refresh

Cleanup after merge (this pass's WORKTREE_ROOT only — § Worktree removal ownership):
# sedea_remove_worktree_folder (absolute path)
# "$HOSTING_ROOT/scripts/compose-worktree-teardown.sh" --worktree-path "$WORKTREE_ROOT"  # warn on failed-open JSON
# worktree-cleanup.sh for that path only — never repo-wide git worktree prune or paths you do not own
```

Example B — **hosting repo** (e.g. `user-auth`; no two-digit stack index unless that repo's rules require it):

```
## Worktree setup

HOSTING_ROOT="$(pwd)"
REPO_BASENAME="$(basename "$HOSTING_ROOT")"
WORKTREES_DIR="$(dirname "$HOSTING_ROOT")/${REPO_BASENAME}-worktrees"
BASE_REF="origin/main"
mkdir -p "$WORKTREES_DIR"

WORKTREE_NAME="improve/logger"
WORKTREE_DIR="${REPO_BASENAME}-$(echo "$WORKTREE_NAME" | tr '/' '-')"
WORKTREE_ROOT="${WORKTREES_DIR}/${WORKTREE_DIR}"
"$HOSTING_ROOT/.sedea/centers/sedea/scripts/worktree-setup.sh" \
  --hosting-root "$HOSTING_ROOT" \
  --worktree-path "$WORKTREE_ROOT" \
  --worktree-name "$WORKTREE_NAME" \
  --base-ref "$BASE_REF"
# sedea_add_worktree_folder — absolute path when attach-required

WORKTREE_NAME="feat/token-auth"
WORKTREE_DIR="${REPO_BASENAME}-$(echo "$WORKTREE_NAME" | tr '/' '-')"
WORKTREE_ROOT="${WORKTREES_DIR}/${WORKTREE_DIR}"
"$HOSTING_ROOT/.sedea/centers/sedea/scripts/worktree-setup.sh" \
  --hosting-root "$HOSTING_ROOT" \
  --worktree-path "$WORKTREE_ROOT" \
  --worktree-name "$WORKTREE_NAME" \
  --base-ref "$BASE_REF"
```

## New worktree from `origin/main`

Do **not** use **`git checkout -b`** on the primary hosting clone for implementation. Create a worktree from **`HOSTING_ROOT`** via center setup (Rule 7 path under `{repo-basename}-worktrees/` — not a direct sibling of **`HOSTING_ROOT`**):

```bash
HOSTING_ROOT="<absolute-hosting-root>"
REPO_BASENAME="$(basename "$HOSTING_ROOT")"
WORKTREES_DIR="$(dirname "$HOSTING_ROOT")/${REPO_BASENAME}-worktrees"
WORKTREE_NAME="<worktree-name>"
WORKTREE_DIR="${REPO_BASENAME}-$(echo "$WORKTREE_NAME" | tr '/' '-')"
WORKTREE_ROOT="${WORKTREES_DIR}/${WORKTREE_DIR}"
mkdir -p "$WORKTREES_DIR"

"$HOSTING_ROOT/.sedea/centers/sedea/scripts/worktree-setup.sh" \
  --hosting-root "$HOSTING_ROOT" \
  --worktree-path "$WORKTREE_ROOT" \
  --worktree-name "$WORKTREE_NAME" \
  --base-ref origin/main
# Mission Control: sedea_add_worktree_folder — absolute path when attach-required
```

If a PR depends on an unmerged predecessor, **wait** for that predecessor to merge into `main` before creating the dependent worktree (unless the developer explicitly requests stacking).

## Keep PRs small and focused

**Normative definition:** [`.sedea/centers/software-development/docs/development-process.md`](../docs/development-process.md) § *PR sizing — test cases and kinds of changes* (center sync contract). This section is the ship-lane summary; **`pr-breakdown`** step 5a applies the full contract during breakdown.

Minimize **two dimensions** independently:

1. **Number of concerns** — a PR should address **one concern** (or as few as possible). A "concern" is a single logical change: one new feature, one improvement, one bug fix. A rename of a single construct touching 500 files is still one concern. Mixing a rename with a behavior change is two concerns and should be two PRs.
2. **Test-case count** — count the **test cases** the PR introduces or meaningfully changes (unit tests, integration / snapshot cases, exploratory recordings — each enumerated case counts once):
   - **≤ 10** — simple PR.
   - **11–20** — mid-sized PR.
   - **21+** — heavy PR; investigate splitting, but do **not** split if the result is a half-shipped feature (Strategy #4 trumps size — see [`development-process.md`](../docs/development-process.md)).

When in doubt, **fewer concerns wins** over smaller test-case count.

### What a "kind of change" is (and why line count is not a size signal)

Raw changed-line count is **not** a size signal in this process. The intuition behind the test-case metric is *kinds of changes*, not lines or files:

- Threading the same prompt fragment into 8 generators is **one kind of change** with 8 instances. A reviewer-agent reads the kind once and glances the other 7 to confirm they're identical — review burden stays at "one kind", not "8×".
- A 2000-line mechanical rename of one construct is one kind of change.
- A 50-line PR that mixes two concerns is still a split candidate (Dimension 1 above).

When estimating PR size for breakdown decisions, count **kinds** first, then enumerate test cases per kind to bucket the PR. If a candidate PR has many instances of the same kind, do **not** split it just to reduce instance count — that produces redundant review work and a half-shipped feature, not a lighter PR.

**Sync:** bucket thresholds and kinds-vs-lines rules must match **development-process.md** § *PR sizing — test cases and kinds of changes* — edit that subsection first when changing metrics.

### Recursive breakdown to PR leaves

Break work down recursively until each piece is a **PR leaf**: one reviewable **concern**, one clear merge base (`origin/main` unless stacking is explicitly requested), not "one line" by default. Prefer **smallest meaningful** slices: each leaf should have a clear behavioral or structural outcome (what changed, how to see it, what tests cover) so reviews are substantive, not noise.

- **Plan tree vs. git worktree** — The **plan** is a tree of intent: **master-planner** roots and nested `.plan.md` files (e.g. `parent:` links), with YAML **todos** in each file for **agents** (Mission Control **Relevant Links** and the operations plan tree surface **plan files only**, not todo rows). **Git worktrees** are execution: at most one active PR per worktree ([One PR per worktree](#one-pr-per-worktree-many-worktrees-in-parallel)). **Each parallel PR** should map to **one leaf plan file** (and honest todos inside that file for the agent) so status stays honest.
- **Child plans** — If one `.plan.md` gets too deep or cross-repo, add a **linked child plan**. The parent keeps a short **Plan map** (bullets or Mermaid) listing child slugs and roles.
- **Contract drift** — If a leaf PR invalidates the breakdown, **update the parent plan** (diagram + todos) before spawning more worktrees so the tree stays the source of truth.
- **Splitting an existing pile** — When splitting after the fact: get explicit approval before worktree edits, commit, or push; save a recoverable snapshot (`git stash create` + `git update-ref refs/backup/pre-split-<timestamp> …`); stage named paths or hunks only (no `git add .` / `git add -A`); use Mermaid when multiple slices. After push, complete **`coding-session`** ship chain through inline **`create-pr`** — never open a PR from other lanes (see **PR creation — inline `create-pr` on `coding-session`** above).

### Planning and validation (diagrams)

For **multi-step or multi-PR** plans, include **at least one Mermaid diagram** (dependency order, data/control flow, or PR slice boundaries). A single trivial PR in one obvious place may omit a diagram. Sequence-heavy work can add a **numbered list** or **PR ↔ module** table alongside Mermaid; prefer Mermaid for **structure** because it renders in GitHub and in-editor. Safe-generation contract: [`.sedea/centers/sedea/docs/mermaid-authoring.md`](.sedea/centers/sedea/docs/mermaid-authoring.md) — opaque ids, sequence `Note` single-line (no `<br/>`, no bare `;`), sequence message labels without bare `;`, flowchart-only `<br/>` in quoted labels.

After each decomposition step, make validation easy for the human: **(a)** diagram, **(b)** ordered leaves marked **independent** vs **depends on …** (see [Plan for independence](#plan-for-independence)), **(c)** explicit **out of scope**. Prefer validation before hosting repo worktrees spin up.

### Situation awareness and operations plans

Do not rely on chat alone for where work stands.

- **Filesystem** — Intent lives in `.sedea/operations/**/plans/*.plan.md`: body, overview, frontmatter **todos**. Runtime state for the **operations plan sidecar union** lives in optional sidecars `.sedea/operations/**/plans/<slug>.state.yaml` (e.g. `worktrees`, `prs`, `session.focusPath`). Plan hierarchy and lifecycle status come from that union — do not assume a fixed roadmap filename; when the workspace ships a roadmap or extension plan under `.sedea/operations/`, resolve it from the user message, Mission Control **Relevant Links**, or `plan-state.mjs list-candidates` (from **`HOSTING_ROOT`**) rather than a hard-coded slug.
- **Agent habits** — Keep todo `status` accurate (`not_started` / `started` / `completed` / `canceled`). Track **postponed** work with a **Postponed / Deferred** subsection or explicit todos so "done" plans do not hide scope. Link related work: a **Related plans** section (slug + one line) in the parent body is enough unless you maintain optional `parent:` / `children:` frontmatter.
- **Discovery** — Use Mission Control **Relevant Links**, lane handover paths, or `plan-state.mjs list-candidates` to see the **plan-file hierarchy** (master-plans, plan nesting, archive state), **lifecycle status**, **worktrees**, and linked PRs; keep plans and sidecars consistent with what those surfaces show. **Forbidden:** instructing developers to open a removed Hub **Plans** pane — that UI was removed; Hub today exposes **Dispatch** and **Centers** only.

### Parallelism vs. batching

Many small PRs mean **many CI runs and many review contexts**. Parallelize when slices are **independent** and touch disjoint areas ([Plan for independence](#plan-for-independence)); **batch** when slices are tightly sequential or would thrash the same hot files. Ordering is not always "smallest first" — sometimes a **foundation or contract** slice unlocks parallel follow-ups.

## Comprehensive PR descriptions

PRs are reviewed by AI agents who compare the description against the code and tests. The description must be the single source of truth for what the PR does:

- State **what** changed and **why**.
- List every behavioral change, new API, schema change, or config change.
- If there are intentional non-changes (things that look like they should change but deliberately don't), call them out.
- Do **not** include a separate test plan section — tests are in the code. The description should be detailed enough that a reviewer can verify tests cover the stated behavior.

### A PR-creating agent prompt and proportional context

The **`create-pr` protocol branch** ([`create-pr/SKILL.md`](../missions/plan-and-deliver/skills/create-pr/SKILL.md)) produces the text **a PR-creating agent** uses to open the PR. That block must give reviewer agents enough **reasoning**, not only diff facts — otherwise comments are often unactionable. **Scale length to PR size**: a small PR gets a **shorter** write-up, but it must still be **complete for one concern**.

Every description starter (including on the first push in this cadence) should cover, in whatever brevity fits the PR:

- **Why this slice** — motivation and how it fits the larger goal (enough to judge intent vs. mistake, especially for improvement slices, guards, or "looks redundant" code).
- **Not in this PR** — deferrals, follow-ups, or parent-epic scope left out on purpose.
- **Plan lineage (when applicable)** — one line with plan slug or path (e.g. `.sedea/operations/**/plans/<slug>.plan.md`) and, if useful, "see Mermaid in plan" so intent is not trapped in chat.
- **Verification pointer** — how to confirm behavior (which tests or observable behavior); still no separate "test plan" essay.

## Hosting-repo PR base branch (binding)

When **`create-pr`** opens a PR on the **hosting repo** (or separate product repo worktree):

1. Resolve **`<prBaseBranch>`** from inline **`baseRef`** — strip remote prefix (typically `origin/` → `main`).
2. **`gh pr create`** **must** include **`--base "<prBaseBranch>" --head "<worktreeName>"`**. **Forbidden:** bare **`gh pr create`** without **`--base`**.
3. **Pre-create self-check:** When **`gh api repos/<owner>/<repo> --jq .default_branch`** ≠ **`<prBaseBranch>`**, **`--base` is mandatory** (always safe to pass even when equal).
4. Executable procedure: [`create-pr/SKILL.md`](../missions/plan-and-deliver/skills/create-pr/SKILL.md) § *`gh pr create` procedure (binding)*.

**Center-repo PRs** (content in a **center git repository**): rule **20** does **not** apply — use [`.sedea/centers/sedea/rules/3_center.mdc`](.sedea/centers/sedea/rules/3_center.mdc) § *Center-repo worktree procedure* and **`development-process.md`** § *Center-repo PR base (binding)*.

## Worktree naming

Names apply to the **hosting repo** (or separate repo) worktree that receives the code change — the **`--worktree-name`** on center **`worktree-setup.sh`** (same string as the git ref the script creates), not necessarily the repo where `.sedea/` planning scripts run. **Worktree setup examples** above are split by repo type — use Example A or B, not both.

### Hosting repo (primary Sedea-enabled hosting repo)

When the **hosting repo** root contains `.sedea/centers/sedea/` on the active workbench — follow **[`.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc`](.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc)** ([`0_hosting-repo.mdc`](.sedea/centers/sedea/rules/0_hosting-repo.mdc) defines the hosting repo).

- Stacked integration on `main`: **`<prefix>/<NN>-<slug>`** with exactly two digits (`01`–`99`). Default prefix for a new stack is **`feat`** unless the plan or developer names another prefix.
- **Invalid on hosting stacked workflow:** `feat/token-auth`, `improve/logger`, `feature/01-foo` — missing `NN`, wrong prefix family, or non-kebab slug.
- Rename the worktree ref locally **before first push** if the worktree name is wrong ([`7_stacked-pr-worktree-naming.mdc`](.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc) § *Before the first git push*).

Benefit-style prefixes (`improve/`, `simplify/`, …) from [`10_plan-naming-convention.mdc`](10_plan-naming-convention.mdc) may use **`<NN>-`** on the primary **hosting repo** when the stack explicitly requires it (e.g. `improve/02-logger`).

### Separate hosting repo worktree

When the worktree is a **separate repository** from the primary hosting repo (e.g. `user-auth`), use descriptive kebab-case worktree names on **that** repo:

- `feat/<short-description>` — new features
- `improve/<short-description>` — improvements (or a more specific verb prefix like `simplify/`, `harden/`, `speedup/`; see [`10_plan-naming-convention.mdc`](10_plan-naming-convention.mdc))
- `fix/<short-description>` — bug fixes

No two-digit stack index unless that repo's own rules or an explicit stacked-PR plan requires it.

If that repo ships worktree-naming rules under `.sedea/centers/<centerSlug>/rules/` or `.cursor/rules/`, follow those for that worktree.

## Review passes (two roles)

Do not conflate **pre-PR review** with **PR-surface review**:

| Role | Session | When | Skill / surface |
|------|---------|------|-----------------|
| **Pre-PR reviewer agent** | **Fresh** child lane — no coding-session transcript carry-over | After implementation approval, commit, and Local test **`deploy-walk`** (when applicable), before **`create-pr`** | [`pre-pr-review/SKILL.md`](../missions/plan-and-deliver/skills/pre-pr-review/SKILL.md) |
| **Reviewer agent** | GitHub PR diff + description (Sedea or third-party) | After PR is open | [`pr-review/SKILL.md`](../missions/plan-and-deliver/skills/pr-review/SKILL.md) for comment triage; not a substitute for pre-PR |

Full definitions: [`.sedea/centers/software-development/docs/development-process.md`](../docs/development-process.md) § *Agents and roles*.

## No agent gcloud secrets or env-var proposals (binding)

Agents **must not** update gcloud secrets, add environment variables to code, or **propose** new env vars. Env vars require an **explicit same-turn developer instruction** naming the variable. Normative detail: [`60_no-agent-env-secrets.mdc`](60_no-agent-env-secrets.mdc).

## PR approve-merge and merge inspect (binding)

Software Development ship lanes (**`coding-session`**, inline **`pr-review`** when merge options apply, and **`plan-reconcile`** fallbacks) inherit the Phase **1** platform contract from [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc) § *Approval-gated agent approve+merge*, § *PR approve-merge structured choice*, and § *Merge inspect procedure*. This rule does **not** duplicate those tables — it binds Software Development ship-lane behavior to rule **6**.

### Modal scope (binding)

Rule **6** § *PR approve-merge structured choice* **modal scope** applies to **every** structured-choice surface in the **current ship chain** that offers agent approve+merge while the PR is **open** (`prState: open`) — not only the first modal after **`create-pr`**. That includes **`coding-session`** post-create-pr, pre-merge authorization, and inline **`pr-review`** disposition gates when agent merge is in scope. Invoker skills own marker placement and option tables; rule **6** binds scope, ordering, and inspect.

### Option ordering (binding)

When agent merge is offered on any Software Development ship-chain modal for **that** open PR:

| Contract | Requirement |
|----------|-------------|
| **Option id** | **`approve-merge-pr`** (canonical cross-center id) |
| **Option label** | **Approve and Merge PR** (exact string — forbid *Merge and Approve*) |
| **Ordering** | **`approve-merge-pr`** is **option index 1**. **`merged-pr-proceed`** (when present) is **option index 2** — never before **`approve-merge-pr`**. Review-first or delegate paths (**`start-pr-review`**, **`start-pr-review-delegate-merge`**, and similar) follow as option 3+ unless the invoker documents a narrower modal. |

Detail and forbidden patterns: rule **6** § *PR approve-merge structured choice*.

### Merge inspect procedure (binding)

After the developer picks **`approve-merge-pr`** (or an equivalent same-turn merge-delegation pick for **that** PR), agents **always inspect before mutating GitHub** — never unconditional approve or merge.

1. **Inspect** — from the worktree or repo root that owns the PR branch:
   ```bash
   gh pr view <number> --json state,mergeable,mergeStateStatus,reviewDecision,statusCheckRollup,url
   ```
2. **Not mergeable** — when **`mergeable`** is not **`MERGEABLE`**, or rollup signals blocked CI, conflicts, or pending required checks: **stop**; do **not** run **`gh pr review --approve`** or **`gh pr merge`** — **except** Cypress-only blockers on **`tapcart-merchant-dashboard`** PRs per § *tapcart-merchant-dashboard Cypress CI carve-out* below. Offer retry / check CI / defer via structured choice on the invoker skill.
3. **Mergeable — approval not required** — run **`gh pr merge`** **only** — do **not** run **`gh pr review --approve`** first.
4. **Mergeable — approval required** — run **`gh pr review --approve`**, then **`gh pr merge`**, in the **same act turn** after the structured-choice pick.

Full algorithm and minimum signals: rule **6** § *Merge inspect procedure*.

**tapcart-merchant-dashboard Cypress CI carve-out (binding):** On PRs targeting **`tapcartinc/tapcart-merchant-dashboard`**, agents **ignore Cypress check failures** for inline **`pr-review`** triage and approval-gated approve+merge on **`coding-session`** lanes — non-Cypress CI, merge conflicts, and unresolved **CHANGES_REQUESTED** remain blocking. Normative detail: **`pr-review/SKILL.md`** § *tapcart-merchant-dashboard Cypress CI carve-out*; **`coding-session/SKILL.md`** § *Inline PR review after PR creation*, [Checkpoint — auto-advance `approve-merge-pr`](#checkpoint--auto-advance-approve-merge-pr-binding), and [Merge procedure](#merge-procedure).

**Checkpoint auto-advance:** when Checkpoint trust auto-advances past a merge modal (implicit **`delegate-merge-confirm`** or equivalent), the **inspect procedure still runs** before any **`gh`** mutate — auto-advance waives the modal, not inspect.

### Post-create modal scope note

On **`coding-session`**, the first structured-choice surface after inline **`create-pr`** completes is a **post-create-pr** gate while the PR remains **open**. That gate is **in scope** for rule **6** modal scope and **`approve-merge-pr`** first ordering above — same as pre-merge and disposition gates later in the chain. PR 2 (**`coding-session`** gate table edits) implements the option rows; this rule **20** cross-ref must exist before those edits cite stable contract text.

### Default path without approve+merge pick

Prefer user merge on GitHub (**`merged-pr-proceed`** / status detect). Agents may offer approval-gated approve+merge only with explicit same-turn structured-choice pick per rule **6** — **forbidden** to infer merge consent from silence, prior turns, or commit/push/create-PR consent.

**Carve-out:** [`.sedea/centers/sedea/skills/promote-submodule-pin/SKILL.md`](.sedea/centers/sedea/skills/promote-submodule-pin/SKILL.md) runs skill-owned **`gh pr merge`** with zero user approval options — distinct from approval-gated mission ship merge; do not copy its auto-merge into Software Development ship lanes.

## Checkpoint step continuation (binding)

This file is the **Software Development ship-lane worktree / commit-push / PR-creation contract**, not a mission protocol with executable ship steps. Under **`trustLevel: checkpoint`**, turn-end modal UX for worktree-open, ship cut-points, pre-PR / create-PR / post-create-pr gates, and post-merge cleanup lives in invoker skills (`coding-session`, `create-pr`, `pre-pr-review`, `plan-reconcile`, `promote-submodule-pin`) and consent gates in sedea rules **6** / **2** / **1** / **7**. **Do not** add literal `USER_CHECKPOINT` markers here for happy-path HOSTING_ROOT resolution, worktree setup hints, ownership-clear remove paths, or policy redirects to **`coding-session`** — that would fight skill auto-advance.

| Surface | Designation | Owner |
|---------|-------------|--------|
| Resolve `HOSTING_ROOT` / script cwd when `.sedea/` ancestors are clear | **auto-advance** | This § *Hosting repo cwd for scripts* |
| Missing hosting root / cannot resolve `.sedea/` | **checkpoint** | This § *Resolve `HOSTING_ROOT`* step 5 — AskQuestion once; no prose-only path guess |
| Center `worktree-setup.sh` + MCP attach / detach after prior consent | **auto-advance** | Invoker / sedea rules **0** / **6** / **7**; this § *Worktree setup in plans* |
| Worktree remove / cleanup when Path A or Path B ownership is clear | **auto-advance** | This § *Worktree removal ownership*; invoker post-merge cleanup |
| Worktree remove / cleanup when ownership is unsure | **checkpoint** | Invoker cleanup / ship skill — strengthened stop prose above; no markers in this file |
| Review-before-commit / ship cut-point / pre-PR / create-PR gates | **baseline-governs** (reference) | **`coding-session/SKILL.md`** Checkpoint turn UX + sedea rule **6** — not duplicated here |
| PR approve-merge modal scope / **`approve-merge-pr`** ordering / merge inspect | **baseline-governs** (reference) | This rule § *PR approve-merge and merge inspect*; rule **6** owns contract |
| Wrong-lane `gh pr create` / create-PR URL handoff | **auto-advance** (policy stop) | This § *PR creation* — redirect to **`coding-session`** ship chain; no prose create-PR substitute |
| Commit and push cadence after same-message / gate consent | **auto-advance** | This § *Commit and push cadence*; sedea rule **6** + skill gates authorize git |
| Center submodule pin promotion after center merge | **auto-advance** | This § *Center submodule pin promotion*; script-backed **`promote-submodule-pin`** (not **`create-pr`**) |
| Turn completion after ship-policy guidance in chat | **baseline-governs** (reference) | Rules **1**–**2**; pointer in Related below |

**Forbidden in this file:** prescribing prose *tell me when* / *come back when* / *I'll wait* / *over to you* / *ask how to proceed* as the primary wait surface for ownership-unsure stops, hosting-root picks, or ship-gate stands-in; duplicating **`coding-session`** / rule-6 AskQuestion option tables; `USER_CHECKPOINT` tokens for happy-path script cwd, worktree setup, or PR-creation policy redirects.

**Related:** Checkpoint step-table template — [`.sedea/centers/sedea/docs/pr-step-table-template.md`](.sedea/centers/sedea/docs/pr-step-table-template.md). Marker syntax — [`.sedea/centers/sedea/docs/user-checkpoint-marker-syntax.md`](.sedea/centers/sedea/docs/user-checkpoint-marker-syntax.md).

## Related

- Worktree ownership / attach-detach: [`.sedea/centers/sedea/rules/0_hosting-repo.mdc`](.sedea/centers/sedea/rules/0_hosting-repo.mdc)
- Commit/push gate: [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc)
- Stacked worktree naming: [`.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc`](.sedea/centers/sedea/rules/7_stacked-pr-worktree-naming.mdc)
- Ship executor: [`coding-session/SKILL.md`](../missions/plan-and-deliver/skills/coding-session/SKILL.md)
- PR approve-merge / merge inspect: [`.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc`](.sedea/centers/sedea/rules/6_git-commit-push-gate.mdc) § *PR approve-merge structured choice* / § *Merge inspect procedure*; this rule § *PR approve-merge and merge inspect*
- Turn completion / no idle handoff: [`.sedea/centers/sedea/rules/2_ask-question-instructions.mdc`](.sedea/centers/sedea/rules/2_ask-question-instructions.mdc); [`.sedea/centers/sedea/rules/1_agent-code-of-conduct.mdc`](.sedea/centers/sedea/rules/1_agent-code-of-conduct.mdc)
- Plan naming cross-ref: [10_plan-naming-convention.mdc](10_plan-naming-convention.mdc)
